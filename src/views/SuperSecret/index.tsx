import React from 'react';
import Header from 'components/Header';

const SuperSecret: React.FC = () => (
  <div>
    <Header />
    Super secret and protected page
  </div>
);

export default SuperSecret;
