/* eslint-disable react/prop-types */

import React, { FunctionComponent } from 'react';

const ScyllaPage: FunctionComponent = ({ children }) => {
  return <div className="margin-2">{children}</div>;
};

export default ScyllaPage;
