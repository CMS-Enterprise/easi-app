package sanitization

// SanitizeHTML takes a string representation of HTML and sanitizes it
func SanitizeHTML(input string) (string, error) {
	output := input // TODO implement sanitizer

	return output, nil
}
